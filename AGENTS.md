# AGENTS.md

This file is intentionally minimal.

Use `CLAUDE.md` as the single source of truth for agent instructions in this repository.

If an external tool only reads `AGENTS.md`, treat it as an alias and apply all rules from `CLAUDE.md`.